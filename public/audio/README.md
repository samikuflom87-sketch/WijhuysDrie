Drop recorded `.mp3` files here, named exactly as referenced by the `audio` field in `src/data/lessons.json` (e.g. `selam.mp3` for `/audio/selam.mp3`).

Until a file exists, its speaker button plays nothing but does not error — the app fails silently on missing audio.
